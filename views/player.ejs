<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>選手データ一覧</title>
</head>
<body>
  <h1>サッカー選手データ一覧</h1>

  <table>
    <tr><th>ID</th><th>背番号</th><th>選手名</th><th>ポジション</th><th>操作</th></tr>
    <% let number = 0; %>
    <% for (let row of data) { %>
      <tr><td><%= number + 1 %></td>
        <td><%= row.number %></td>
        <td><a href="/player/<%= number %>"><%= row.name %></a></td>
        <td><%= row.position %></td>
         <td><a href="/player/delete/<%= number %>" onclick="return confirm">削除</a></td>
      </tr>
      <% number += 1; %>
    <% } %>
  </table>
  <a href="/player/create">追加</a>
</body>
</html>
